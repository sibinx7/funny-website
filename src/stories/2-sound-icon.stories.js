import React from 'react';
import { action } from '@storybook/addon-actions';
import { Button } from '@storybook/react/demo';

export default {
  title: "Funny application"
}

export const SoundIcon = () => {
  return <SoundIcon mute={false}/>
}

